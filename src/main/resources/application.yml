spring:
  datasource:
    url: jdbc:mysql://localhost:3306/esercitazione
    username: root
    password: gTYù=(iA5?&_/
  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: create-drop
  jackson:
    serialization:
      fail-on-empty-beans: false
server:
  port: 5050